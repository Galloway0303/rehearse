# Screenshots

PNG assets used in the project README.

| File | Description |
|------|-------------|
| `overlay.png` | Chinese cover + word capture concept |
| `home.png` | Control panel home |
| `practice.png` | Write-back practice |

HTML sources (`mock-*.html`) can be re-rendered with a headless browser if you change the mock UI:

```bat
chrome --headless=new --window-size=1280,820 --screenshot=home.png file:///path/to/mock-home.html
```
